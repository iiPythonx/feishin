import { Stack } from '@mantine/core';
import { Tweaks } from './iipython-tweaks';

export const TweaksTab = () => {
    return (
        <Stack spacing="md">
            <Tweaks />
        </Stack>
    );
};
